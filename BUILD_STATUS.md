# حالة المشروع — EXD | Elawaady XDigital

**الفرع:** `chatgpt/store-build` · **آخر تحديث:** 01/09/2026

`main` ما اتلمسش. `elawaady.com` ما اتفتحش ولا اتبعتله أي طلب.

---

## المختصر

المنصة اتبنت واتختبرت. **الشيء الوحيد الناقص عشان يبقى فيه رابط معاينة هو
بيانات الاستضافة** — مش موجودة في المشروع ولا في CI، وكل workflow في المستودع
بيقول صراحة إنه بيتحقق فقط ولا ينشر.

أول ما تبعت بيانات الاستضافة: `php tools/install.php --admin=owner` وخلاص.
الأمر ده اتجرّب فعليًا على قاعدة فاضية والنتيجة متجر شغّال.

---

## الأرقام

| | |
|---|---|
| جداول قاعدة البيانات | 55 |
| Migrations | 20، كلها إضافية |
| ملفات PHP | 82 (~15,570 سطر) |
| صور المتجر | 275 ملف (15 ميجا WebP، الأصل كان 173 ميجا PNG) |
| اختبارات تمر | 143 + الفحص الثابت |
| CI workflows خضراء | 7 من 7 |

---

## اللي اتنفذ

### الحسابات
تسجيل ودخول وخروج واستعادة كلمة مرور شغّالة على قاعدة حقيقية. الجلسة زوج
selector/validator، والـvalidator متخزّن مشفّر — قراءة جدول الجلسات ما تسمحش
بتزوير كوكي. رسالة الخطأ واحدة سواء الإيميل مش موجود أو الباسورد غلط. CSRF على
كل كتابة. إيقاف مؤقت بعد 5 محاولات. تغيير كلمة المرور بينهي كل الجلسات التانية.

**نوعان فقط:** مستخدم ومورد. `platform_users.account_type` نوعه
`ENUM('user','supplier')` — التاجر مش موجود ومش ممكن يتضاف. أي حساب قديم بصفة
تاجر بيتحوّل مورد وبيحتفظ بصفته القديمة في `legacy_account_label`.

### الموردون
المورد يسجّل ويفضل `pending` — يقدر يدخل ويكمّل بياناته وبس. الإدارة تعتمده،
فيقدر يقدّم خدمة، والإدارة تعتمد الخدمة فتتنشر كخدمة حقيقية بسعر بيع المتجر،
وسعر المورد يفضل في مكان ما يوصلوش أي استعلام بيشوفه العميل، والخدمة تتنشر
غير نشطة عشان تتراجع قبل ما تتباع.

**بيانات العميل ما بتوصلش للمورد إطلاقًا.** استعلام لوحة المورد ما بيختارش اسم
ولا هاتف ولا بريد العميل — الحماية في الاستعلام نفسه مش في التمبليت.

### الصلاحيات
7 أدوار و24 صلاحية. الكود بيسأل «هل ده مسموح له يأكّد دفعة؟» مش «هل ده المسؤول
المالي؟» — فتغيير مين له إيه تعديل في مصفوفة الأدوار مش في الصفحات. القائمة
بتعرض اللي الشخص يقدر يفتحه بس، وكل صفحة بتتحقق بنفسها كمان.

آخر مدير عام ما ينفعش يتشال من دوره — ده هيقفل الباب على الكل.

### الطلبات والمحفظة
السعر بيتحسب على السيرفر من صف الخدمة. أي سعر جاي من المتصفح بيتجاهل — متختبر.
الرصيد ما بيتعدلش مباشرة أبدًا: كل حركة سطر ثابت في الدفتر بيحمل الرصيد اللي
بعدها، والتصحيح حركة عكسية مش تعديل. الطلب المدفوع من المحفظة عملية واحدة
متكاملة: الطلب والخصم والدفعة والسجل، وإما تتم كلها أو ما تتمش.

الطلب ما يقدرش ينط لحالة غير مسموحة — الرسم البياني معرّف مرة واحدة، وحتى مزود
خارجي بيقول «Completed» ما يقدرش يتخطاه.

### الوساطة
حجز المبلغ بيخصم من المشتري ويسجّله محجوز. البائع ما ياخدش حاجة قبل التحرير.
التحرير بيدّي البائع قيمة الصفقة والرسوم تفضل للمنصة. الاسترداد بيرجّع الاتنين
كاملين. بعد كل ده الدفتر لسه بيساوي الرصيد بالظبط. صفحة وساطة عامة برقم
الترخيص 767-766-857.

### اللوحة
26 صفحة. كل صفحة بتعلن الصلاحية اللي محتاجاها وبتتحقق من CSRF token. مفيش حساب
إدارة افتراضي ولا كلمة مرور في المستودع — بتتعمل من الطرفية.

**تعديل بنية قاعدة البيانات من اللوحة اتشال.** كان فيه ملف بيعمل `ALTER TABLE`
من غير أي جلسة ولا صلاحية ولا تسجيل — أي زائر يعرف الرابط كان يقدر يغيّر البنية.
دلوقتي البنية بتتغيّر من الطرفية بس عبر `migrate.php`، اللي بيرفض أي `DROP` أو
`TRUNCATE` أو `DELETE FROM` وبيرفض يشتغل لو ملف مطبّق اتعدّل بعد كده.

**الإعدادات بقت صفوف في قاعدة البيانات.** كانت ملف JSON مكتوب فوقه إنه «ما
بيلمسش قاعدة البيانات» — يعني إعدادات ما بتعيشش بعد النشر. وألوان الثيم بتستبدل
متغيّرات نظام التصميم مش قواعد `!important` مصوّبة على أسماء كلاسات.

### الواجهة
الصفحة الرئيسية بقت حلقة على صفوف `homepage_sections`. الترتيب والإظهار والشكل
وعدد الكروت أعمدة — إخفاء قسم أو تحريكه تعديل في اللوحة. متختبر: إخفاء قسم
الأسئلة الشائعة شاله من الصفحة، وتغيير ترتيبه حرّكه.

القسم اللي ما عندوش محتوى ما بيظهرش أصلًا. صور المتجر كلها داخلة، من غير قص —
الصف بياخد شكل الصورة اللي فيه بدل ما يقصّها في إطار جاهز.

### المحتوى
18 صفحة سياسة كانت نصوص مكتوبة جوه 18 ملف PHP — يعني تعديل كلمة يحتاج نشر.
بقت صفوف تتعدّل من اللوحة. السياسات لها إصدارات، والموافقة عليها بتتسجّل: أي
إصدار ومين ومن أي عنوان وإمتى — ولأن الإصدار ما بيتعدلش، اللي وافق عليه العميل
يفضل مقروء زي ما اتعرض عليه.

---

## بجد شغّال؟ آه — 143 اختبار

مش فحص syntax. اختبارات بتشتغل على HTTP حقيقي وكوكيز حقيقية وقاعدة MariaDB
حقيقية، وبتشتغل في CI على كل push.

| | |
|---|---|
| `tests/auth_flow.sh` | 32 |
| `tests/admin_smoke.sh` | 55 |
| `tests/order_flow.sh` | 25 |
| `tests/supplier_mediation_flow.sh` | 31 |
| `tests/bind_param_check.php` | فحص ثابت لـ128 استعلام |
| `tests/responsive_sweep.js` | 12 صفحة × 3 مقاسات |

**الاختبارات لقت باجات حقيقية.** أهمها: `order_create.php` كان بيربط 22 متغيّر
بـ21 حرف نوع — يعني **مفيش حد كان يقدر يشتري أي حاجة**، وفحص الـsyntax ما
بيمسكهاش لأنها بتظهر وقت التنفيذ بس. و8 صفحات في اللوحة كانت بتقبل الكتابة من
أي حد داخل ومن أي موقع خارجي.

وثلاث مرات لقطة الشاشة بانت كأنها باج وما كانتش: لقطة الصفحة الكاملة في
Chromium بتعيد الرسم على شاشة ضخمة والصور تحت الطية بترجع غير مرسومة — إطارات
فاضية شكلها زي عطل CSS بالظبط. الأداة بقت تصوّر شاشة شاشة زي ما الزائر بيشوف.

---

## اللي محتاج منك

| المطلوب | بيفتح إيه |
|---|---|
| **بيانات الاستضافة** | **النشر ورابط المعاينة** |
| `APP_ENCRYPTION_KEY` | تخزين مفاتيح مزوّدي SMM |
| مفاتيح مزوّدي API | سحب الخدمات والتنفيذ التلقائي |
| بوابة دفع | الدفع الأونلاين (الدفع اليدوي والمحفظة شغّالين دلوقتي) |
| SMTP | رسائل استعادة كلمة المرور بالبريد |
| قائمة الأسعار | الأسعار الحقيقية |

---

## حاجات معروفة

**كل الخدمات بسعر صفر وبتظهر «حسب الطلب».** دي حالتها في مشروعك الأصلي — كل
`INSERT` في `database.sql` بيحط `price` صفر. مش باج، وما اخترعتش أسعار. تتحط من
اللوحة.

**صور الأقسام لسه أيقونات.** عندك 13 صورة قسم و13 بنر قسم في `uploads/`، بس
مفيش في مشروعك ما يربط أي صورة بأي قسم. ما خمّنتش. الربط من شاشة تعديل القسم في
اللوحة.

**الخدمات الـ20 بتلبس بنرات 3:1 مش صور مربعة.** ده الربط اللي في مشروعك أنت.
عندك كمان 76 صورة مربعة مخصوصة لكروت الخدمات في `uploads/services/cards/` من
غير ربط بخدمات — نفس السبب.

**استعادة كلمة المرور بتولّد رابط سليم بس ما بتبعتش إيميل.** محتاجة SMTP.

**مجلد `backend/` بايثون ساكت زي ما هو.** ما اتحذفش وما اتبنيش عليه — PHP هي
الـruntime زي ما اتحدد.
