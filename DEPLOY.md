# النشر على Staging

المتجر جاهز للتثبيت. الخطوات دي اتجرّبت فعليًا على قاعدة فاضية تمامًا
(MariaDB 10.11 + PHP 8.4) والنتيجة: 43 قسم، 20 خدمة، 14 قسم في الصفحة
الرئيسية، 24 صلاحية، 20 migration، وحساب إدارة شغّال.

**ممنوع تشغيل أي حاجة من ده على `elawaady.com`.** السكريبتات نفسها بترفض:
`bootstrap.php` بيقف لو `APP_ENV=production`، و`tools/install.php` بيقف لو
`APP_URL` بيشاور على `elawaady.com` أو أي subdomain منه.

---

## 1. المتطلبات

| | الأقل | ملاحظة |
|---|---|---|
| PHP | 8.1 | مطلوب `mysqli` · `mbstring` · `openssl` · `curl` · `fileinfo` |
| MySQL / MariaDB | MySQL 8 أو MariaDB 10.6 | **مش SQLite** |
| مساحة | ~60 ميجا | منها 15 ميجا صور المتجر |

---

## 2. متغيّرات البيئة

تتضبط في لوحة الاستضافة (Environment Variables). **ولا واحد منها يتكتب في
الكود ولا يترفع على GitHub.**

```
APP_ENV=staging
APP_URL=https://<staging-domain>

DB_HOST=localhost
DB_PORT=3306
DB_NAME=<اسم القاعدة>
DB_USER=<المستخدم>
DB_PASS=<كلمة المرور>

# مفتاح تشفير مفاتيح مزوّدي الـAPI. لازم يتولّد مرة واحدة ويتحفظ:
#   php -r "echo bin2hex(random_bytes(32));"
APP_ENCRYPTION_KEY=<64 حرف hex>
```

لو الاستضافة مبتدعمش Environment Variables، اعمل ملف `.env` جنب
`index.php` بنفس المفاتيح. الملف ده في `.gitignore` ومش بيترفع.

> `APP_ENCRYPTION_KEY` لو اتغيّر بعد ما اتخزّن مفتاح مزوّد، المفاتيح المخزّنة
> مش هتفكّ. ولّده مرة واحدة واحتفظ بيه.

---

## 3. التثبيت

```bash
git clone -b chatgpt/store-build <repo> .
php tools/install.php --check      # بيقول هيعمل إيه من غير ما يعمل حاجة
php tools/install.php --admin=owner
```

`--check` بيتأكد إن القاعدة فاضية ويعرض الـmigrations اللي هتتطبّق من غير ما
يكتب أي حاجة. الأمر التاني بينفّذ، وبيسأل عن كلمة مرور حساب الإدارة في
الطرفية — عشان ما تتحفظش في history ولا في ملف.

**قاعدة فيها بيانات؟** `bootstrap.php` هيرفض، وده صح. شغّل بس:

```bash
php migrate.php --status    # إيه المطبّق وإيه المعلّق
php migrate.php             # يطبّق المعلّق فقط
```

`migrate.php` بيرفض أي ملف فيه `DROP TABLE` أو `TRUNCATE` أو `DELETE FROM`،
وبيرفض يشتغل لو ملف اتطبّق قبل كده واتغيّر بعدها.

---

## 4. صلاحيات الملفات

```bash
chmod -R 755 uploads
chown -R <web-user>:<web-group> uploads
```

`uploads/` هي المكان الوحيد اللي السيرفر محتاج يكتب فيه.

---

## 5. التأكد قبل ما تفتح للناس

```bash
php staging_check.php          # البيئة والملفات الأساسية
php tests/bind_param_check.php # فحص ثابت لكل استعلام
```

وبعد ما الموقع يبقى شغّال على الدومين:

```bash
BASE=https://<staging-domain> tests/auth_flow.sh
BASE=https://<staging-domain> ADMIN_PASS=<كلمة المرور> tests/admin_smoke.sh
BASE=https://<staging-domain> ADMIN_PASS=<كلمة المرور> tests/order_flow.sh
```

الاختبارات دي بتنشئ حسابات وطلبات حقيقية. شغّلها على staging بس.

---

## 6. أول خطوات في اللوحة

اللوحة على `/admin/login.php`.

1. **إعدادات المنصة** — واتساب الدعم والبريد. الفوتر وصفحة التواصل بيقروا منها.
2. **الهوية البصرية** — الشعارات ورقم الترخيص.
3. **أقسام الرئيسية** — ترتيب الصفحة وإظهار وإخفاء كل قسم.
4. **أماكن العرض** — الخدمات اللي تظهر في «الأكثر مبيعًا» و«الأكثر استخدامًا».
5. **الخدمات** — الأسعار. كل الخدمات دلوقتي بسعر صفر وبتظهر «حسب الطلب»،
   وده حالتها في المشروع الأصلي. الأسعار قرار تجاري بيتحط من هنا.
6. **الفريق والصلاحيات** — حسابات باقي الفريق:
   `php tools/create_admin.php --username=NAME --role=ROLE`

---

## 7. الأمان

- مفيش حساب إدارة افتراضي ولا كلمة مرور في المستودع.
- تغيير بنية القاعدة من الطرفية بس. مفيش صفحة في اللوحة بتعمل DDL.
- مفاتيح مزوّدي الـAPI بتتخزّن مشفّرة AES-256-GCM، وما بتظهرش في أي صفحة.
- بيانات العميل ما بتوصلش للمورد: استعلام لوحة المورد ما بيختارش اسم ولا
  هاتف ولا بريد العميل أصلًا.
- كل كتابة في اللوحة بتتأكّد من CSRF token وصلاحية.
- بعد أول دخول، غيّر كلمة مرور الإدارة من الطرفية:
  `php tools/create_admin.php --username=owner --reset-password`

---

## 8. اللي لسه محتاج منك

| المطلوب | بيفتح إيه |
|---|---|
| بيانات الاستضافة | النشر نفسه |
| `APP_ENCRYPTION_KEY` | تخزين مفاتيح مزوّدي SMM |
| مفاتيح مزوّدي الـAPI | سحب الخدمات والتنفيذ التلقائي |
| بيانات بوابة الدفع | الدفع الأونلاين المباشر (الدفع اليدوي والمحفظة شغّالين) |
| بيانات SMTP | إرسال رسائل استعادة كلمة المرور بالبريد |
| قائمة الأسعار | الأسعار الحقيقية بدل «حسب الطلب» |
